/**
 * @deprecated Prefer StudyWorkspace. Kept so any residual imports fail loudly
 * at typecheck if still referenced — re-export the new workspace.
 */
export { StudyWorkspace as StudyChat } from "./study-workspace";
